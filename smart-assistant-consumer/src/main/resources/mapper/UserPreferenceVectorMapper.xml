<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smartassistant.consumer.mapper.UserPreferenceVectorMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.smartassistant.consumer.entity.UserPreferenceVector">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="vector_type" property="vectorType"/>
        <result column="content" property="content"/>
        <result column="embedding_id" property="embeddingId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 根据用户ID和向量类型查询 -->
    <select id="findByUserIdAndType" resultMap="BaseResultMap">
        SELECT * FROM user_preference_vectors
        WHERE user_id::text = #{userId}::text
          AND vector_type = #{vectorType}
        LIMIT 1
    </select>

    <!-- 根据用户ID查询所有向量 -->
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM user_preference_vectors
        WHERE user_id::text = #{userId}::text
        ORDER BY vector_type
    </select>

    <!-- 根据 embeddingId 查询 -->
    <select id="findByEmbeddingId" resultMap="BaseResultMap">
        SELECT * FROM user_preference_vectors
        WHERE embedding_id = #{embeddingId}
        LIMIT 1
    </select>

    <!-- 删除用户的指定类型向量 -->
    <delete id="deleteByUserIdAndType">
        DELETE FROM user_preference_vectors
        WHERE user_id::text = #{userId}::text
          AND vector_type = #{vectorType}
    </delete>

    <!-- 删除用户的所有向量 -->
    <delete id="deleteByUserId">
        DELETE FROM user_preference_vectors
        WHERE user_id::text = #{userId}::text
    </delete>

</mapper>
