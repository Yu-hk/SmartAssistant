# 2026-09 分析报告核对与第一批整改

输入：用户提供的 `project-assessment-2026-09.md`（以 PR #50 / 2ae59702 为基线）。
本次核对包含 PR #51–53 后的代码，不把报告里的建议当成已实现功能。

## 核对结果

| 报告项目 | 当前代码事实 | 本轮处理 |
| --- | --- | --- |
| 手册版本、职责漂移 | 确认；还含不存在的 Recommend 服务 | 重写上下文，自动生成依赖/模块/镜像事实并加 CI |
| 手册自动注入 System Prompt | 当前 PromptManager 无 loadProjectContext 方法 | 移除过时陈述，不宣称发生运行时提示词注入 |
| 9 个服务 / 11 模块 / 8 服务 | 报告前后计数矛盾；实际 8 服务 + 3 库 | 由根 POM 生成模块清单 |
| Nacos 2.3 | Compose 服务端 3.1.0、POM 客户端 3.2.0 | 区分客户端、镜像声明与实际线上版本 |
| 推荐未读画像 | GraphNodeExecutionService 已注入可选画像，默认等待 500ms | 保留现有链路，不重复接入 |
| 三套画像必须合并 | PG 电商画像、Redis 实体事实、Agent 文件记忆职责不同 | 定义治理边界，暂不搬数据或删调用方 |
| 真实集成测试缺失 | 已有 PG/MQ/Redis 测试；PG 会自动连接业务库且开启后失败仍跳过 | 隔离测试库，严格失败，新增 PG/MQ CI 与执行报告检查 |
| legacy 原型 | frontend/server 存在；原默认 dev 会启动它 | 默认 dev 仅 Vite，旧入口改为显式 legacy 命名，保留历史文件 |
| 框架升级风险 | 当前声明版本确认，未核实完整升级动机 | 补当前基线 ADR、验证矩阵与可执行发布回滚检查单，不降级 |

## 下一批：需要单独回归与发布的运行时代码改造

1. 画像/记忆：盘点 Consumer 同步/SSE/MQ，以及 OrderMemoryTool、ProductMemoryTool、
   ContextOrchestrator、MemoryExtractor；统一来源、版本、冲突与清除规则，再引入查询门面。
   PG 快照是电商画像事实源，Redis 投影可丢失重建。禁止将文件记忆直接覆盖有版本快照。
2. SmartReActAgent：先做逐事件行为基线，再把迭代预算/停止判定、工具执行、消息组装分别抽出。
   保留工具确认、取消、Token 统计、失败回退契约；不在治理 PR 中一次重写千行核心引擎。
3. 可选适配器和生产故障演练：MCP/Milvus、RabbitMQ 选主、PG 全迁移、跨实例恢复另做专项。
   本轮真实服务容器契约测试不是完整生产可靠性认证。

以上是明确保留的后续工作，不计入本轮已完成功能。

后续进展：画像来源标记、PG 只读门面、旧记忆工具隔离与文件边界已进入第二批，
详见 [画像与记忆治理](profile-memory-governance.md)。没有迁移历史数据；
全量删除协议、跨进程文件并发及 ReAct 引擎拆分仍未完成。

## 验证记录

本轮不修改 Java 主代码、业务提示词、数据库迁移或前端页面代码。
因此没有新的运行产物需要切换，也不为文档/CI 变更重启生产应用。
本地实测：11 项 Java 测试通过（PG 配置安全 3、真实 pgvector 7、真实 RabbitMQ 1），零跳过。
PG 负向验证使用本机不可用端口：7 项全部报连接错误、零跳过、BUILD FAILURE，符合严格失败要求；
随后恢复专用 PG 重新执行 10 项全部通过。7 项 Python 治理/报告检查测试通过，前端生产构建通过。
测试用 PG/MQ 容器及网络已按独立 Compose 项目清理，未操作业务数据库。

服务器只读盘点确认应用容器运行中；线上与本地 `index.html` SHA-256 均为
`7acb7afde91a2d1691dd3027872431b189e0e28b03787b4b894c17053c167286`。
本机访问演示站 HTTPS 曾连接重置，不能将其等同于业务服务故障。
经已校验主机密钥的 SSH 在服务器本机访问 nginx HTTPS `/healthz`，整体、Redis、服务发现和 readiness 均返回 UP。
本轮未更换线上产物，保留 PR #53 版本。CI 结果以本轮 PR 的检查记录为准。
