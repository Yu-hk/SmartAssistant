# 最新备份保留与 ReAct 执行预算拆分（2026-09-20）

## 空间清理

用户明确要求备份只保留最近一次。在清理前核对所有运行及停止容器的挂载、当前产物和上一版带画像恢复保护的回滚产物；删除只发生在 `/opt/smart-assistant/backups` 中逐个列明并复核的旧目录。未清理生产卷、旧日志、独立控制目录或发布目录中的挂载文件。

- 删除 125 个旧文件，共 21,871,329,734 字节（约 20.37 GiB），逐文件路径、大小、SHA-256 留在 `releases/react-boundary-20260920/backup-retention.json`。用户已确认没有其他备份，旧恢复点不可找回。
- 唯一保留备份集 `backups/latest-20260920`：约 3.26 GiB，包含最新 PostgreSQL dump、前端与部署配置、私有容器配置、当前及最近有效回滚 JAR 的独立文件副本和校验清单。目录权限 0700，私有配置 0600，不提交 Git。
- 新 dump 在禁网、无生产挂载、临时内存 PostgreSQL 中以 `pg_restore --exit-on-error --no-owner --no-privileges` 完整恢复成功，37 张 public 表；没有输出真实用户正文。核验容器已清除。
- 本备份不包含 Redis/MQ/Nacos 卷的一致性快照，也不包含独立删除控制目录的回滚副本；不是全站或整机灾备认证。画像恢复必须保留当前固定来源控制记录，再按生产删除规程重放，不能直接开流量。
- 清理后可用空间从约 1.2 GiB 增至 19 GiB，根分区使用率从 99% 降至 76%。后续新发布产物会占用部分空间。

## 执行预算边界

从 `SmartReActAgent` 提取请求独立的 `AgentExecutionBudget`，集中计时、累计输入/输出 Token 及预算判断；使用单调时钟计算耗时，避免系统时间校准干扰。错误回复、指标采集、取消传播、迭代顺序仍由原循环处理。

保留原有严格 `>` 判定：达到预算值不立即中止，超过后在下一次循环边界停止；关闭 Token 跟踪时不累计、不触发预算停止。超时仍是循环边界检查，不会强制取消正在执行的供应商 HTTP 请求。不是整个 ReAct 引擎已重写，也未改变实际业务工具的幂等策略。

验证：本地 69 项 Agent 测试、54 项跨模块回归、3 项严格报告校验器和 15 项发布产物校验器测试通过；相同 common 包在服务器禁网 JVM 中 69 项测试零失败、零中止、零跳过。新增 8 项预算单测纳入 CI 最低执行数门禁。

## 部署与公网复测

发布目录 `releases/react-budget-20260920`。四个服务包及内嵌 common 在本地、服务器重建及容器启动后核对哈希；暂停 Gateway 并排空派发/画像提交队列及运行中会话后切换。Consumer、Router、Product、Order、Gateway 健康及公开 `/healthz` 全部通过。没有数据库迁移或前端修改。

- common SHA-256：`b5fa59f737b9e91ede358c45148d71b637671c92156fefb8e5f99e19fd3ee7fe`。
- Consumer：`bde745f3ca3dd57e6d1670231d57c7efcf89107fefc19405bfc1fc796c8d2a1c`。
- Router：`3456da937137e42e6583114da7776c287b03d263a1f06b5ad5286cd7459cb3b7`。
- Product：`ad868672f18aa0779eebfed9dccb794949f3595b6bbf08155f4e56be21b07b3a`。
- Order：`db31388481fe76442f1337902104bc99ec832f97b78d676b27e104eb7ff5d332`。

公网独立测试账号完成“AirPods Pro多少钱？有货吗？”以及同会话追问“这个规格是多少”：价格命中 1999 元，规格回答未夹带颜色；两轮均包含工具、Token、done 事件，没有错误、超时或暂停事件。匿名隐私接口返回 401，登录后接口可用；测试会话关闭 200，令牌撤销并删除私有临时凭据文件。本轮未执行订单写入、退款或画像删除，也未做真实浏览器视觉验收。

保留本轮之前的四个带控制保护的运行版本作为回滚容器；独立目录、固定来源 pin 和读写权限未改动。最终可用空间约 18 GiB、使用率 78%。发布证据 `deployment.json` 与 `public-verification.json` 保存在上述发布目录。
