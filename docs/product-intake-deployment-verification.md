# 商品录入提取：部署与线上验收

2026-09-14，演示站 [商品录入入口](https://xiaoyuai.cloud/admin/products)。已部署前端、Consumer 和两列增量迁移；本轮没有提交或推送 Git。

## 发布范围

- Consumer 在商品录入时从简介、规格提取明确的重量、续航及场景、主动降噪，确认后与商品、原文及审计记录一起保存。
- 前端新增独立管理员“商品录入”页面；用户端没有新增管理入口。
- 执行 `20260914_add_product_intake.sql`，仅添加 `description`、`feature_ingestion_audit`，无历史商品回填。
- Gateway 暂停接收请求、等待聊天队列排空后切换 Consumer，健康后恢复入口。没有更换 Product、Router、Gateway 等其他服务包。
- 更新前备份 Consumer、前端和商品表；新增列兼容旧应用，应用回滚不需要删列。
- 首页、登录页、管理录入页及 29 个静态资源通过服务器访问 HTTPS 演示域名校验，内容与本地构建逐文件一致；旧哈希资源保留，兼容已打开的标签页。

发布 SHA-256：

| 对象 | SHA-256 |
| --- | --- |
| Consumer | `22ffea7d8c22d0a3f4d0a68bce7b81e403ed4a67b65bb691ac66ca2e7d253e2a` |
| 前端 index.html | `8238e48332333baae04c64d8a60cac45c50932f3b1448e52cb8be484ea9bef80` |
| Product（保持原版） | `f25d16a309af0fc49e67bb9012bc013d48b17785cf1d303ead69f93ff9f24769` |

## 验证结果

部署前 Consumer/Gateway 84 条测试、前端 4 条测试及生产构建通过。桌面、手机的本地生产构建交互测试在上一轮已通过。

线上接口、数据库和对话链路共 **39 项检查通过**：

1. 原有 318 条商品迁移前后数量、内容指纹一致；两个新列存在。
2. 预览从原文得到 1200g、视频播放续航 12h、明确不支持主动降噪，且没有创建商品。
3. 模糊描述、通话降噪和冲突参数不被猜测成结构化事实；过长输入返回 400。
4. 未确认的已知参数、负数、缺续航场景、数字冒充布尔值均返回 400，没有残留商品。
5. 确认录入返回 201，新 PostgreSQL 连接可读到商品、参数、原始简介、操作人、版本和提取依据。
6. 原文 SHA-256 可重算，审计记录明确 `externalVerification=false`；人工更正后的值和最初提取值均保留。
7. 未知参数可全 null 入库，false 不会变成 null。
8. 重复编码（含大小写变化）返回 409，不覆盖价格；两个并发创建返回 201/409，仅保存一条。
9. 创建使用本次提交的最新原文；重复执行迁移不改变已存数据。
10. Product 实际查询新录入商品，正确返回重量、续航、降噪、来源，执行 SUCCEEDED、质量 PASS。
11. 公网匿名伪造管理员头返回 401；通过真实注册/登录获取的普通用户 JWT 即使附带伪造管理员头，提取和创建均返回 403。
12. 公网普通用户完整对话返回 AirPods Pro 价格 1999 元、库存充足；请求 COMPLETED，4 次工具调用成功，Token 共 17974（输入 15217、输出 2757）。测试会话已关闭，没有创建订单或提交评价。

4 条虚构商品均使用正常推荐排除的 `E2E-PROD-` 前缀、缺货状态和“禁止售卖”标记。检查完成后已按精确编码及名称/品类条件删除；原有 318 条商品未改变。数据库输出中的 `DELETE 4` 是删除命令状态，不是第五条商品。

测试账号 `intakecheck_20260914214018`（用户 ID 1126），会话 `intake-smoke-511f0d28c940460f93720bd101f68bfc` 已关闭；测试对话保留供管理审计，密码和 JWT 不写入验收文件。

最终收尾：Consumer、Product、Gateway 和演示域名健康状态均为 UP，14 个容器运行，任务/画像/工作流队列及对应死信队列均为 0。部署文件校验和一致，目录仍为 318 条，临时录入审计行已清空；磁盘剩余约 44 GiB，使用率 43%。首次收尾脚本假设容器 Name 带 `/` 前缀而中止，改为按容器名称直接查询地址后复测通过；没有因此重启服务。

## 验收边界

- 管理员业务验收通过服务器内网 Consumer 的可信身份头完成，**没有完成公网管理员账号登录及浏览器实际录入**。普通用户越权测试经过真实 HTTPS 网关与 JWT 校验。
- 本机直连演示域名仍出现 HTTPS 连接重置；服务器侧访问同一域名、静态资源、登录和对话均正常。本次没有修改域名、网络或认证设置来规避该差异。
- 首版是规则提取，不是通用大模型语义抽取；原文没有明确事实时仍需人工补充。正式目录没有自动生成未经核实的参数。
- 直接执行 SQL 的历史种子/导入不会自动调用 Java 录入服务。已有推荐缓存 TTL 策略未在本次改变。

## 回滚与保留策略

最新回滚文件：`/opt/smart-assistant/backups/before-product-intake-20260914/` 下的 `consumer.jar`、`frontend.tar.gz`、`products.dump`，SHA256SUMS 和归档清单均已验证。

按“只保留最近一次备份”要求，已清理本次相关组件的上一次 Consumer、前端和商品表备份，共 301136324 字节（约 287 MiB）；其他服务备份未动。删除的旧备份无法直接恢复，当前切换前版本和商品表仍可从上述最新备份回滚。前一次持久化部署文档中的旧 Consumer/商品表备份路径已被本次取代。

本地验收证据：

- `.codex-artifacts/product-intake-deployment-20260914.log`
- `.codex-artifacts/product-intake-live-verification-20260914.json`
- `.codex-artifacts/product-intake-chat-20260914.events.json`
- `.codex-artifacts/product-intake-final-health-20260914.json`
- `.codex-artifacts/product-intake-backup-retention-20260914.log`

原始对话虽然返回正确业务事实，但仍附带了与当前商品无关的知识库排除说明；这不影响本次录入、持久化和遥测验收，不应据此声称回复简洁度也已完全优化。
