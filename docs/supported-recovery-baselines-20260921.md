# 受支持基线与画像跨存储恢复边界

## 已确认范围

2026-09-21 用户确认：按“当前生产备份 + 明确列出的受支持基线”交付，未知历史版本拒绝自动升级。
这取代无法提供逐版本快照证据的“全历史版本升级”目标，不代表全站/整机灾备认证。

| 基线 | 允许输入 | 验证路径 |
| --- | --- | --- |
| `consolidated-20260920` | 固定摘要的仓库 schema 与 seed | 独立 PG16 初始化、27 个有序迁移、漂移拒绝、逻辑备份恢复及业务契约 |
| `production-backup-20260920` | 已清点的最新 `database.dump` | 固定摘要与 PGDMP 格式校验；37 表隔离恢复已有记录；恢复后另做独立控制重放与派生清理 |
| 其他版本 | 不支持 | 拒绝自动执行，先补可信快照、迁移记录和隔离验收，再评审登记 |

摘要常量保存在 `scripts/recovery_baseline.py`，不会从待恢复文件中自动计算后信任。
仓库 SQL 仅允许 Git 的 CRLF/LF 转换，其他变化需要重新评审；生产 dump 按原始字节验证。
后续备份轮换不是自动升级白名单：新备份须重新清点、隔离恢复并更新固定摘要。

```sh
python3 scripts/recovery_baseline.py --baseline consolidated-20260920 --repo .
python3 scripts/recovery_baseline.py --baseline production-backup-20260920 \
  --backup /opt/smart-assistant/backups/latest-20260920/database.dump
```

以上仅验证输入，不连接数据库、不执行迁移、不启动流量。离线生产控制重放也强制校验此白名单。
不能仅凭参数名认定目标数据库来源；实际还原记录、固定控制来源、完整控制链与 schema 契约必须一致。
生产备份没有可信的逐迁移账本，不对其盲目重跑全部历史迁移；合并 schema 的 27 项迁移门禁不等于该备份的迁移历史。

## 本轮修复与联合测试

- 独立控制记录完整但 `profile_lifecycle` 单独回滚、丢失或被重新开启时，画像恢复保护拒绝使用旧画像。
  已删除账号的控制墓碑仍保留，不要求为已删除账号重建生命周期。
- 清理任务必须有全部五项成功收据才能显示 `ONLINE_CLEANED`；状态接口也核验收据，不能信任回滚后残留的完成标志。
- 新增真实 PostgreSQL、Redis、RabbitMQ、POSIX 文件组合测试：旧画像与缓存、未确认消息重投、未知文件阻塞、清理协调器重启、五项收据收敛。
  检查其他用户的画像与文件、原始聊天均保留，重复投递不会重新生成已删除画像。
- CI 在同一作业启动专用三种存储；显式检查集成用例执行数量，跳过不算通过。

联合测试使用合成数据和引用消息，注入回滚后的状态；真正 `pg_dump` 的完整性由独立数据库恢复门禁覆盖。
未回滚真实生产 PG/Redis/MQ/文件，未恢复旧订单消息，未承诺分布式 exactly-once。
同机控制目录不覆盖整机/磁盘丢失，历史日志原件保留策略不变。

## 发布证据

服务器证据目录：`/opt/smart-assistant/releases/cross-store-20260921`。

- 旧版保护逻辑在新增两项用例中均失败：回滚/重新开启生命周期时未拒绝；失败日志保留为 `before-guard-v7.log`。
- 最终 Consumer JAR 及其内嵌 common、真实 PG/Redis/RabbitMQ/POSIX 的 14 项测试通过，零失败、中止、跳过；`candidate.log`、`candidate-proof.json` 记录结果与产物摘要。临时容器与内存目录均已清除。
- 30 项 Python 防护测试全部通过（Linux 无跳过）；固定生产备份摘要验证通过，没有写生产数据库。
- `baseline-upgrade.json`：27 个迁移、39 表逻辑恢复、数据/结构/序列比较、重入/漂移/原子回滚与控制重放通过；演练使用 `python3 -O`，临时数据库已移除。
- 本地 Java 24 项通过，1 项 Linux 软链接用例在 Windows 跳过，已在上述 14 项服务器测试中真实执行；四服务构建及内嵌 common 一致性检查通过。
- 测试环境初期遇到 RabbitMQ 目录/探针身份、启动未就绪和握手超时，修正测试环境配置、检查实际监听状态及有界握手等待后再验证。没有降低业务断言或把跳过计为成功。

四服务已切换，Consumer、Router、Product、Order 及 Gateway 健康检查通过，公开 `/healthz` 正常。
发布先暂停 Gateway、确认派发/画像提交队列 ready/unacked 及执行中会话均为零，再切换产物。
没有数据库迁移或真实数据还原；环境变量、镜像、资源限额、日志配置、内部 DNS `10.89.1.1`、
独立控制目录与 Consumer 可写/其他三服务只读挂载保持不变，旧的带恢复保护容器及 JAR 保留可回退。

| 产物 | SHA-256 |
| --- | --- |
| common | `af118ca2134f3815f073a0078f68f4f5849cdbf79ed909a09c96be6213d7576e` |
| consumer | `afb5365efbcab9fabe306155d13ef3413a2431118e299ab737c1756f99d79844` |
| router | `041d29271810698e7b41476884eadefccd86fa05f54cdf28a7dc46ec5dae2969` |
| product | `5a22d6ea29695ab70dc9f617ace4eb3dc3279547ade94910947da370dc0c09b4` |
| order | `97ab84ae1ece8476c2776213fb8e0b9b87a59b055647e67972fa3ed7d5b768ff` |

仅删除本轮中断上传的 `probe.zip`（210,993,152 字节），逐一核对未被任何容器挂载；它是可重建临时测试包，不是备份。最新备份及旧日志未删除。

公开接口验收通过：新建专用测试账号先完成 AirPods 价格库存咨询，再通过公开隐私接口请求画像删除，
五项收据均为成功；重复请求返回同一任务，分析保持关闭。随后在同一会话询问规格，正常返回且没有额外颜色。
两轮都有 `tool_usage`、`token_usage`、`done`，没有错误或暂停事件。会话关闭 HTTP 200，测试令牌撤销验证通过，私有认证文件已移除。
仅删除该专用测试账号的画像，未对现有真实用户发起删除。`public-verification.json` 与 `deployment.json` 保存验收状态。

本轮“受支持基线 + 画像跨存储恢复保护”范围已完成服务器验收；未验证的浏览器视觉、全站/整机恢复等仍按分析报告单独列示，不据此扩大承诺。

## CI 补充回归

首次 PR 检查中，真实跨存储与控制归档测试均通过，但两个原有 `ProfileControlJournalTest` 测试桩
未提供新增生命周期查询的返回值，返回 null 后被新保护正确拒绝。补齐“正常一致”的明确返回值，
同时增加返回不一致数量和 null 都拒绝的断言，没有放宽生产保护或删除失败用例。
在服务器用相同已部署 JAR 补跑日志、文件与跨存储共 23 项测试，全部通过，零跳过；
证据为 `candidate-ci.log`，临时容器和目录均已清除。本次后续变更只有测试与文档，没有重新替换生产 JAR。
